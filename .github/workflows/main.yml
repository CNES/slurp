name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '**'
  workflow_dispatch:

jobs:
  build-env:
    name: Run CI
    runs-on: ubuntu-latest
    steps:
      - run: echo "------ Building environment ... ------"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Dependencies
        shell: bash
        run: pip install -e .[dev]

      - run: echo "------ Environment builded ! ------"
      
      - run: echo "------ passing formaters ... ------"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Black & Isort
        run: |
          black --check --diff slurp tests
          isort slurp tests --check --diff
        continue-on-error: true
      
      - run: echo "------ formaters done ! ------"

      - run: echo "------ Starting Pylint Analysis ... ------"

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        shell: bash
        run: |
          pip install .[dev]
          pip install pygments

      - name: Run Pylint
        shell: bash
        run: |
          pylint --recursive=y --disable=all --fail-under=10 --enable=too-many-statements,too-many-nested-blocks slurp

      - name: Run McCabe Complexity Check
        shell: bash -l {0}
        run: |
          ./continuous_integration/scripts/check_mccabe_complexity.sh 25 slurp

      - run: echo "------ Pylint Analysis done ! ------"
      - run: echo "------ Starting CI tests ... -------"

      - name: CI Tests
        shell: bash
        run: |
          pytest -m ci --cov=slurp --cov-report=xml:.ci-reports-slurp/coverage_slurp.xml --cov-report html:cov_html_slurp --cov-report=term --junitxml=pytest-results-slurp.xml --config config_tests_ci.json --main_config main_config_tests_ci.json -s
      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-slurp
          path: |
            .ci-reports-slurp/
            cov_html_slurp/
            pytest-results-slurp.xml

      - run: echo "------ CI tests done ! -------------"
